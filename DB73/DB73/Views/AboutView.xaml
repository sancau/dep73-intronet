<UserControl x:Class="DB73.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary Source="_resources.xaml"/>
    </UserControl.Resources>

    <ScrollViewer   ScrollViewer.VerticalScrollBarVisibility="Auto"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.CanContentScroll="True">


        <StackPanel Orientation="Vertical" HorizontalAlignment="Left"  Margin="40,20,0,10">

            <Label
                   Content="О программе" 
                   Foreground="{StaticResource PrimaryTitleBrush}"
                   FontSize="24" 
                />

            <Label 
                   Foreground="{StaticResource PrimaryTitleBrush}"
                   Content="Информация о программном продукте " 
                   FontSize="14"
                   Margin="0,5,0,10"
                />
        
            <TextBlock Text="{Binding About}" 
                       FontSize="12" 
                       TextWrapping="Wrap" 
                       IsEnabled="False" 
                       Margin="5,20,0,0"
                       />
        </StackPanel>

    </ScrollViewer>
</UserControl>
